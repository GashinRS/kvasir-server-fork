package kvasir.services.api.solid.vocab

object SolidVocab {
    const val baseUri = "http://www.w3.org/ns/solid/terms#"

    const val inserts = "${baseUri}inserts"
    const val deletes = "${baseUri}deletes"
    const val where = "${baseUri}where"
}