<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>kg-changes-api</artifactId>
    <version>${revision}</version>

    <parent>
        <groupId>kvasir-services</groupId>
        <artifactId>api-services-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../api-services/pom.xml</relativePath>
    </parent>

    <dependencies>
        <dependency>
            <groupId>kvasir-libs</groupId>
            <artifactId>utils</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>kvasir-plugins</groupId>
            <artifactId>common-http-extensions</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>kvasir-plugins</groupId>
            <artifactId>clickhouse-knowledge-graph</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>kvasir-plugins</groupId>
            <artifactId>kafka-messaging</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>kvasir-plugins</groupId>
            <artifactId>default-knowledge-graph</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>kvasir-libs</groupId>
            <artifactId>test-utils</artifactId>
            <version>${revision}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>kvasir-services</groupId>
            <artifactId>init-service</artifactId>
            <version>${revision}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>kvasir-services</groupId>
            <artifactId>kg-change-processor</artifactId>
            <version>${revision}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>kvasir-plugins</groupId>
            <artifactId>default-knowledge-graph</artifactId>
            <version>${revision}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
