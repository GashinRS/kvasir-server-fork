# Kvasir hurl test variables for CI (Docker-in-Docker environment).
# Services are reachable via the 'docker' hostname (DinD alias).
# This file is git-tracked — it contains no secrets.

keycloak_url=http://docker:8280
realm=kvasir
kvasir_url=http://docker:8080
kss_vocab=https://kvasir.discover.ilabt.imec.be/vocab#
