# Kvasir hurl test variables for local dev mode (just dev-services-up + ./mvnw compile quarkus:dev).
# Both Kvasir (28080) and all backing services run on 28xxx ports.
# This file is git-tracked — it contains no secrets.

keycloak_url=http://localhost:28280
realm=kvasir
kvasir_url=http://localhost:28080
kss_vocab=https://kvasir.discover.ilabt.imec.be/vocab#
