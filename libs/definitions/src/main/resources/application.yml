kvasir:
  http:
    base-uri: http://localhost:8080/
    webclient-uri: ${kvasir.http.base-uri}_ui/

"%dev":
  kvasir:
    http:
      base-uri: http://localhost:28080/
  quarkus:
    http:
      port: 28080
    log:
      level: INFO
      category:
        "kvasir":
          level: DEBUG
          min-level: DEBUG
        "idlab":
          level: DEBUG
          min-level: DEBUG
        # Reduce log level for Kafka Streams to WARN to avoid excessive logging during development
        "org.apache.kafka.streams":
          level: WARN
          min-level: WARN

"%test":
  kvasir:
    http:
      base-uri: http://localhost:28180/
  quarkus:
    http:
      port: 28180
    log:
      level: INFO
      category:
        "kvasir":
          level: DEBUG
          min-level: DEBUG
