kvasir:
  http:
    base-uri: http://localhost:8080/
    webclient-uri: ${kvasir.http.base-uri}_ui/

"%dev":
  quarkus:
    log:
      level: INFO
      category:
        "kvasir":
          level: DEBUG
          min-level: DEBUG
        "idlab":
          level: DEBUG
          min-level: DEBUG

"%test":
  quarkus:
    log:
      level: INFO
      category:
        "kvasir":
          level: DEBUG
          min-level: DEBUG
