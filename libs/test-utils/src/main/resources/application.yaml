"%test":
  quarkus:
    http:
      access-log:
        enabled: true
    # management:
    #   port: 9101
    #   test-port: 9002
    oidc:
      enabled: false
    keycloak:
      admin-client:
        client-id: admin-cli
        realm: master
        username: admin
        password: admin
        grant-type: PASSWORD
  kvasir:
    base-uri: http://localhost:${quarkus.http.test-port}/

