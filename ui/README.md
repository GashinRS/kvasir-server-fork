# KvasirUi

## Usage

`npm start`

* Go to keycloak: http://locahost:8280
* login with `admin:admin`
* go to realm alice
* create user alice
* add credentials to alice
* add realm role `owner` to alice

You can now login with alice on her `alice` pod with the UI at http://localhost:4200
