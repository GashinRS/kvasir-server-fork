apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kvasir-ui.fullname" . }}-test-connection"
  labels:
    {{- include "kvasir-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kvasir-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
