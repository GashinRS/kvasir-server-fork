# .gitlab-ci.yml

include:
  - local: /.ci/templates.yml
  - local: /.ci/maven.yml
  - local: /.ci/compose.yml
  - local: /.ci/docs.yml
  - local: /.ci/release.yml
  - local: /.ci/timoni.yml
  - local: /.ci/kind.yml

variables:
  GIT_STRATEGY: clone
  GIT_CLEAN_FLAGS: -ffdx
  INSTANCE: "Writerside/kd"
  ARTIFACT: "webHelpKD2-all.zip"
  ALGOLIA_ARTIFACT: "algolia-indexes-KD.zip"
  ALGOLIA_APP_NAME: "23BLLNEZ2S"
  ALGOLIA_INDEX_NAME: "prod_KVASIR-NEXT"
  CONFIG_JSON_PRODUCT: "kd"
  CONFIG_JSON_VERSION: "main"
  MAVEN_CLI_OPTS: >-
    --batch-mode
    --errors
    --fail-at-end
    --show-version
    -DinstallAtEnd=true
    -Dstyle.color=always
  MAVEN_OPTS: >-
    -Dhttps.protocols=TLSv1.2
    -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository
    -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN
    -Dorg.slf4j.simpleLogger.showDateTime=true
    -Djava.awt.headless=true
    -XX:+HeapDumpOnOutOfMemoryError
    -XX:HeapDumpPath=target/
  CI_TOOLS_REGISTRY: "gitlab.ilabt.imec.be:4567/discover/ci-tools"
  # renovate: datasource=docker depName=registry.jetbrains.team/p/writerside/builder/writerside-builder
  WRITERSIDE_IMAGE: "${CI_TOOLS_REGISTRY}/writerside-builder:2025.04.8412"
  # renovate: datasource=docker depName=eclipse-temurin
  QUARKUS_JIB_BASE_JVM_IMAGE: "${CI_TOOLS_REGISTRY}/eclipse-temurin:25_36-jre-noble"
  # renovate: datasource=docker depName=gitlab.ilabt.imec.be:4567/discover/ci-tools
  CI_TOOLS_IMAGE: "${CI_TOOLS_REGISTRY}:4.1.1"
  QUARKUS_JIB_PLATFORMS: "linux/amd64,linux/arm64/v8"
  QUARKUS_CONTAINER_IMAGE_PUSH: "false"
  QUARKUS_CONTAINER_IMAGE_REGISTRY: $CI_REGISTRY
  QUARKUS_CONTAINER_IMAGE_GROUP: $CI_PROJECT_PATH
  QUARKUS_CONTAINER_IMAGE_USERNAME: $CI_REGISTRY_USER
  QUARKUS_CONTAINER_IMAGE_PASSWORD: $CI_REGISTRY_PASSWORD

stages:
  - test
  - package
  - verify
  - deploy
  - release

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TITLE =~ /^(\[Draft\]|\(Draft\)|Draft:)/
      when: never
    # Releasaurus release MR (source branch `releasaurus-release-<base>`):
    # publish a floating `next` tag alongside the per-commit image. This is
    # what verify:compose / verify:kind fall back to in dependency-only MRs
    # (which don't trigger package:maven). The tag refreshes whenever the
    # release MR's pipeline runs, so it always reflects the latest
    # release-candidate build.
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^releasaurus-release-/'
      variables:
        IMAGE_TAG: "$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA"
        IMAGE_ADDITIONAL_TAG: "next"
        QUARKUS_CONTAINER_IMAGE_PUSH: "true"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      variables:
        IMAGE_TAG: "$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA"
        IMAGE_ADDITIONAL_TAG: "$CI_COMMIT_REF_SLUG"
        QUARKUS_CONTAINER_IMAGE_PUSH: "true"
    # Always run a pipeline on the default branch, even when an MR is open.
    # This is required for release-pr and release:publish to trigger after a release MR is merged.
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    # Suppress redundant branch pipelines when an MR is open for that branch.
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    # Release pipelines are triggered by tags; build an image with the tag as version and a `latest` tag for convenience.
    # Only release from semVer tags (v0.21.0) — guards against arbitrary tags overwriting `latest`.
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$/'
      variables:
        IMAGE_TAG: "$CI_COMMIT_TAG"
        IMAGE_ADDITIONAL_TAG: "latest"
        QUARKUS_CONTAINER_IMAGE_PUSH: "true"
    - if: $CI_COMMIT_BRANCH
