apiVersion: v1
kind: Service
metadata:
  name: {{ include "kvasir-ui.fullname" . }}
  labels:
    {{- include "kvasir-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kvasir-ui.selectorLabels" . | nindent 4 }}
