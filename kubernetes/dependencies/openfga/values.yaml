replicaCount: 1
datastore:
  engine: postgres
  applyMigrations: true
  migrationType: initContainer
  # Reference to the secret created by pgo
  uri: postgres://pgauth.auth:5432/openfga
  existingSecret: openfga.pgauth.credentials.postgresql.acid.zalan.do
  secretKeys:
    usernameKey: "username"
    passwordKey: "password"
