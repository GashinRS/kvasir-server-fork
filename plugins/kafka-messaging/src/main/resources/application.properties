kvasir.messaging.kafka.bootstrap-servers=localhost:9092
kvasir.messaging.kafka.advertised-hostname=localhost
kafka.bootstrap.servers=${kvasir.messaging.kafka.bootstrap-servers}
kafka.advertised.hostname=${kvasir.messaging.kafka.advertised-hostname}
